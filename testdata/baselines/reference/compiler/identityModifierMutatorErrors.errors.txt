identityModifierMutatorErrors.ts(4,18): error TS100012: 'identity' modifier can only appear on a function type with no parameters.
identityModifierMutatorErrors.ts(5,26): error TS100017: 'mutator' modifier cannot be used with 'identity' modifier.
identityModifierMutatorErrors.ts(23,5): error TS100014: Identity narrowing was conservatively dropped at an uncertainty boundary. Add an explicit guarded temporary or refactor to keep the narrowing scope local.
identityModifierMutatorErrors.ts(24,11): error TS2322: Type 'string | undefined' is not assignable to type 'string'.
  Type 'undefined' is not assignable to type 'string'.


==== identityModifierMutatorErrors.ts (4 errors) ====
    // Phase 5: Mutator validation diagnostics and error cases
    
    // === Section 1: mutator and identity cannot combine ===
    type BadCombo1 = identity mutator (v: string) => void; // error: can't combine
                     ~~~~~~~~
!!! error TS100012: 'identity' modifier can only appear on a function type with no parameters.
    type BadCombo2 = mutator identity () => void; // error: can't combine
                             ~~~~~~~~
!!! error TS100017: 'mutator' modifier cannot be used with 'identity' modifier.
    
    // === Section 2: Valid mutator declarations ===
    type GoodMutator1 = mutator (v: string) => void;
    type GoodMutator2 = mutator (a: number, b: string) => void;
    type GoodMutator3 = mutator <T>(v: T) => void;
    type GoodMutator4 = mutator () => void; // valid — parameterless mutator like reset()
    
    // === Section 3: mutator in generic position ===
    interface Container<T> {
        read: identity () => T;
        write: mutator (v: T) => void links read;
    }
    
    declare function createContainer<T>(initial: T): Container<T>;
    const c = createContainer<string | undefined>("hello");
    if (c.read() !== undefined) {
        const val: string = c.read(); // OK — narrowed
        c.write(undefined);
        ~~~~~~~~~~~~~~~~~~
!!! error TS100014: Identity narrowing was conservatively dropped at an uncertainty boundary. Add an explicit guarded temporary or refactor to keep the narrowing scope local.
        const afterWrite: string = c.read(); // should error — mutator invalidated
              ~~~~~~~~~~
!!! error TS2322: Type 'string | undefined' is not assignable to type 'string'.
!!! error TS2322:   Type 'undefined' is not assignable to type 'string'.
    }
    